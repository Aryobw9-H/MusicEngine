<Window x:Class="MusicEngine.App.BatchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:MusicEngine.App.ViewModels"
        Title="Queue a list" Width="560" Height="520"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgBrush}"
        FontFamily="{StaticResource UiFont}"
        Closing="Window_Closing">
    <Grid Margin="20,18,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Queue a list" FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource TextBrush}"/>
        <TextBlock Grid.Row="1" Text="One song per line — names (Finglish or Persian) or links. Each line is resolved, then you pick what gets queued."
                   FontSize="11.5" Foreground="{StaticResource FaintTextBrush}" Margin="0,3,0,12" TextWrapping="Wrap"/>

        <TextBox Grid.Row="2" x:Name="QueriesBox" Style="{StaticResource Input}"
                 Height="110" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Text="{Binding QueriesText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 IsEnabled="{Binding CanEdit}"/>

        <TextBlock Grid.Row="3" Text="{Binding Progress}" FontSize="11.5" Margin="0,10,0,6"
                   Foreground="{DynamicResource AccentBrush}"/>

        <ListBox Grid.Row="4" Background="Transparent" BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 ItemsSource="{Binding Items}">
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:BatchItemViewModel}">
                    <Grid Margin="0,2,0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" Style="{StaticResource Switch}" VerticalAlignment="Center"
                                  IsChecked="{Binding IsSelected, Mode=TwoWay}"/>
                        <StackPanel Grid.Column="1" Margin="8,0,0,0">
                            <TextBlock Text="{Binding Query}" FontSize="11"
                                       Foreground="{StaticResource FaintTextBrush}" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding MatchLabel}" FontSize="12.5" Margin="0,1,0,0"
                                       Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,12,0,0">
            <Button Content="Resolve" Style="{StaticResource BtnAccent}" Padding="18,6"
                    Command="{Binding ResolveCommand}"/>
            <Button Content="Cancel resolving" Style="{StaticResource BtnGhost}" Margin="8,0,0,0"
                    Command="{Binding CancelResolveCommand}"
                    Visibility="{Binding IsResolving, Converter={StaticResource BoolToVisibility}}"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Close" Style="{StaticResource Btn}" Margin="0,0,8,0" Click="Close_Click"/>
            <Button Content="Queue selected" Style="{StaticResource BtnAccent}" Padding="18,6"
                    Command="{Binding QueueSelectedCommand}"/>
        </StackPanel>
    </Grid>
</Window>
